spring:
  datasource:
    url: jdbc:mysql://localhost:3306/database-desafio
    username: root
    password: Pablo#3008
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  application:
    name: wicket-frontend

server:
  port: 8080
